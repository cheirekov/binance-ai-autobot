# Ports exposed on the host machine
API_PORT=8148
UI_PORT=4173

# Bind API port to this interface (set to 0.0.0.0 to expose publicly)
API_HOST_BIND=127.0.0.1

# If true, /health does not require x-api-key after setup (not recommended for public deployments)
API_PUBLIC_HEALTH=false

# Mainnet safety: even if UI Live trading is enabled, MAINNET live orders are blocked unless this is true.
ALLOW_MAINNET_LIVE_TRADING=false

# Live execution safety (applies when Live trading is enabled in UI)
# NOTE: after setup, these are fallback values only. Runtime controls are saved in config.json and editable in UI Advanced.
# - Cooldown between real orders (milliseconds)
LIVE_TRADE_COOLDOWN_MS=60000
# - Max quote notional per order (in your home stable coin units, e.g. 25 USDT/USDC)
LIVE_TRADE_NOTIONAL_CAP=25
# - Used only for a conservative balance check before placing a MARKET order
LIVE_TRADE_SLIPPAGE_BUFFER=1.005
# - Prevent immediate sell-back of the same pair base after a fresh buy (milliseconds)
LIVE_TRADE_REBALANCE_SELL_COOLDOWN_MS=900000
# - Conversion sizing buffers used when converting non-home assets to top up home stable balance
CONVERSION_BUY_BUFFER=1.005
CONVERSION_SELL_BUFFER=1.002
CONVERSION_FEE_BUFFER=1.002

# Container runtime
DATA_DIR=/data

# UI server -> API service URL (Docker-internal)
API_BASE_URL=http://api:8148

# Integrations
BINANCE_BASE_URL=https://api.binance.com
NEWS_FEEDS=https://cointelegraph.com/rss,https://cryptoslate.com/feed/,https://decrypt.co/feed
NEWS_FETCH_TIMEOUT_MS=8000
