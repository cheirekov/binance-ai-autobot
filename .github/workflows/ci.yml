name: CI

on:
  pull_request:
  push:
    branches: ["main"]

jobs:
  docker-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run lint/test/build in Docker
        run: docker compose -f docker-compose.ci.yml run --rm ci
